<nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top">
  <div class="container">

    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center gap-2" href="/listings">
      <i class="fa-solid fa-mountain-city"></i>
      <span class="fw-bold">AtithiStay</span>
    </a>

    <!-- Toggler -->
    <button 
      class="navbar-toggler" 
      type="button" 
      data-bs-toggle="collapse" 
      data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Content -->
    <div class="collapse navbar-collapse" id="navbarNav">

      <!-- LEFT -->
      <div class="navbar-nav align-items-lg-center">
        <a class="nav-link fw-semibold" href="/listings">Explore</a>
      </div>

      <!-- CENTER SEARCH -->
      <div class="mx-lg-auto my-3 my-lg-0">
        <form class="d-flex" role="search">
          <input
            class="form-control rounded-pill px-4"
            type="search"
            placeholder="Search stays"
            aria-label="Search"
            style="width:260px;">
        </form>
      </div>

      <!-- RIGHT -->
      <div class="navbar-nav ms-lg-auto align-items-lg-center gap-3">

        <% if (currUser) { %>
          <a class="nav-link fw-semibold" href="/listings/new">
            Make it Stay!
          </a>
        <% } %>

        <% if (!currUser) { %>
          <a class="nav-link fw-bold" href="/signup">Sign up</a>
          <a class="nav-link fw-bold" href="/login">Login</a>
        <% } else { %>
          <a class="nav-link fw-bold" href="/logout">Logout</a>
        <% } %>

      </div>
    </div>

  </div>
</nav>